{
  "name": "@fieldflow/scheduling",
  "private": true,
  "scripts": { "dev": "vite", "build": "vite build", "test": "node --test tests/*.test.js" },
  "devDependencies": { "playwright": "latest" }
}
